# requirements.txt
# =================
# All Python packages our project depends on.
# Install with: pip install -r requirements.txt
#
# Railway reads this file automatically and installs everything before deploying.

# Core Django framework
Django==5.2.1

# Django REST Framework — turns Django into an API server
djangorestframework==3.15.2

# JWT authentication for DRF
# simplejwt handles token creation, signing, validation, and refresh
djangorestframework-simplejwt==5.3.1

# PostgreSQL database driver (psycopg2-binary = pre-compiled, easier to install)
psycopg2-binary==2.9.10

# Parse DATABASE_URL string into Django DATABASES dict
dj-database-url==2.2.0

# CORS — allows React frontend to call our Django API from a different port
django-cors-headers==4.4.0

# Read .env file / environment variables cleanly
python-decouple==3.8

# Production WSGI server (replaces Django's dev server)
gunicorn==22.0.0

# Serve static files in production without nginx
whitenoise==6.7.0
